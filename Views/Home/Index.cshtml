@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Home – Inventory Demand Optimizer</title>

  <!-- Bootstrap & FontAwesome -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css"/>
  <link href="https://fonts.googleapis.com/css?family=Calistoga" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"/>

  <style>
    /* Global top‐navbar */
    .topbar-global {
      margin-bottom: 0;
      border-radius: 0;
      background: #fff;
      border-bottom: 1px solid #ddd;
    }
    .topbar-global .navbar-brand { font-weight: bold; }
    .topbar-global .navbar-nav>li>a { font-size:1rem; }

    body { font-family: 'Calistoga', cursive; background: #e8f0fe; }

    /* Sidebar */
    .sidebar {
      width:220px; background:#000; color:#fff;
      height:100vh; position: fixed; top:0; bottom:0;
      transition:width .3s; overflow:hidden; font-size:18px;
    }
    .sidebar.collapsed { width:60px; }
    .sidebar-header { padding:15px; text-align:center; }
    .sidebar-header i.fa-bars { font-size:28px; color:#fff; cursor:pointer; }
    .sidebar ul { list-style:none; padding:0; margin-top:20px; }
    .sidebar ul li { padding:15px; white-space:nowrap; }
    .sidebar ul li:hover, .sidebar ul li.active { background:#1e1e1e; }
    .sidebar a { color:#fff; text-decoration:none; display:flex; align-items:center; }
    .sidebar a i { width:24px; text-align:center; }
    .sidebar.collapsed ul li a span { display:none; }

    /* Main wrapper */
    .main-wrapper {
      margin-left:220px; margin-top:50px; padding:30px;
      transition:margin-left .3s;
    }
    .main-wrapper.collapsed { margin-left:60px; }

    /* Local topbar */
    .topbar {
      position:absolute; top:10px; right:20px;
      display:flex; align-items:center;
    }
    .topbar .logout {
      cursor:pointer; color:#555; font-size:2.5rem; margin-right:20px;
    }
    .topbar .logout:hover { text-decoration:underline; }
    .topbar i.fa-user-circle {
      font-size:3rem; color:#333; cursor:pointer;
    }

    /* Welcome */
    .welcome-msg { font-size:2.8rem; color:#333; margin-bottom:25px; }

    /* Intro panel */
    .intro-panel {
      background:#fff; border-radius:8px;
      box-shadow:0 2px 8px rgba(0,0,0,0.1);
      padding:30px; line-height:1.6;
    }
    .intro-panel h2 { font-size:3rem; margin-bottom:20px; color:#222; }
    .intro-panel h3 { font-size:2rem; margin-top:30px; color:#444; }
    .intro-panel p { font-size:1.4rem; margin-bottom:20px; color:#444; }
    .intro-panel ul { font-size:1.3rem; margin-left:20px; }
    .intro-panel ul li { margin-bottom:12px; }
  </style>
</head>
<body>

  <!-- Sidebar -->
  <div class="sidebar" id="sidebar">
    <div class="sidebar-header">
      <i class="fa fa-bars" onclick="toggleSidebar()"></i>
    </div>
    <ul>
      <li class="active">
        <a asp-controller="Home" asp-action="Index">
          <i class="fa fa-home"></i> <span>Home</span>
        </a>
      </li>
      <li>
        <a asp-controller="Dashboard" asp-action="Index">
          <i class="fa fa-dashboard"></i> <span>Dashboard</span>
        </a>
      </li>
      <li>
        <a asp-controller="Inventory" asp-action="Index">
          <i class="fa fa-briefcase"></i> <span>Inventory</span>
        </a>
      </li>
      <li>
        <a asp-controller="Suppliers" asp-action="Index">
          <i class="fa fa-truck"></i> <span>Suppliers</span>
        </a>
      </li>
      <li>
        <a asp-controller="Sales" asp-action="Index">
          <i class="fa fa-file"></i> <span>Sales Orders</span>
        </a>
      </li>
      <li>
        <a asp-controller="PurchaseOrders" asp-action="Index">
          <i class="fa fa-shopping-cart"></i> <span>Purchase Orders</span>
        </a>
      </li>
      <li>
        <a asp-controller="Reports" asp-action="Index">
          <i class="fa fa-bar-chart"></i> <span>Data Viz</span>
        </a>
      </li>
      <li>
        <a asp-controller="DataModel" asp-action="Index">
          <i class="fa fa-sitemap"></i> <span>Data Model</span>
        </a>
      </li>
      <li>
        <a asp-controller="About" asp-action="Index">
          <i class="fa fa-info-circle"></i> <span>About Us</span>
        </a>
      </li>
    </ul>
  </div>

  <!-- Main wrapper -->
  <div class="main-wrapper" id="mainWrapper">
    <div class="topbar">
      <span class="logout" onclick="$('#logoutModal').modal('show')">Logout</span>
      @* <i class="fa fa-user-circle" onclick="openProfileModal()"></i> *@
    </div>

    <div class="intro-panel">
      <h2>Welcome to the Inventory Demand Optimizer</h2>
      <p>Effective inventory management is crucial for businesses seeking operational efficiency and financial health. Without accurate forecasting and control, businesses risk overstocking—tying up valuable capital and increasing storage costs—or understocking, leading to lost sales and dissatisfied customers. The Inventory Demand Optimizer Web Application addresses these critical inventory challenges by leveraging advanced machine learning techniques to accurately forecast product demand, enabling businesses to manage inventory levels proactively and precisely.</p>

      <h3>What Does This Application Offer?</h3>
      <p>Our innovative application integrates powerful machine learning algorithms with a user-friendly interface, designed specifically to simplify inventory management tasks. It helps businesses anticipate customer demand with greater accuracy, optimize inventory levels, and reduce unnecessary overhead costs.</p>
      <p>Here's a comprehensive breakdown of each feature:</p>

      <h3>Inventory Management</h3>
      <ul>
        <li><strong>Create:</strong> Add new products with name, description, category, pricing, quantity, reorder points, and supplier details.</li>
        <li><strong>Read:</strong> Browse product lists showing stock levels, pricing, and reorder status.</li>
        <li><strong>Update:</strong> Quickly adjust quantities, pricing, or specs to keep data current.</li>
        <li><strong>Delete:</strong> Remove obsolete or discontinued items.</li>
      </ul>

      <h3>Supplier Management</h3>
      <ul>
        <li><strong>Create:</strong> Add supplier profiles with contact info and delivery schedules.</li>
        <li><strong>Read:</strong> Review supplier details at a glance.</li>
        <li><strong>Update:</strong> Keep supplier data up to date.</li>
        <li><strong>Delete:</strong> Remove outdated suppliers.</li>
      </ul>

      <h3>Sales & Purchase Orders</h3>
      <ul>
        <li><strong>Create:</strong> Submit new sales/purchase orders with customer or supplier details.</li>
        <li><strong>Read:</strong> Track order status and history.</li>
        <li><strong>Update:</strong> Modify orders as needed.</li>
        <li><strong>Delete:</strong> Cancel or remove erroneous orders.</li>
      </ul>

      <h3>Dashboard & Reports</h3>
      <p>A real-time dashboard presents key metrics across inventory, sales, and purchases. Export detailed reports for deeper analysis and strategic decision making.</p>
    </div>
  </div>

  <!-- Profile Modal -->
  <div id="profileModal" class="modal fade" tabindex="-1">
    <div class="modal-dialog"><div class="modal-content">
      <div class="modal-header"><h4>Profile Settings</h4></div>
      <div class="modal-body">
        <label>Full Name</label>
        <input type="text" id="profileName" class="form-control" value="@User.Identity.Name"/><br/>
        <label>Email</label>
        <input type="email" id="profileEmail" class="form-control" value="@User.FindFirst("email")?.Value"/>
      </div>
      <div class="modal-footer">
        <button class="btn btn-primary" onclick="saveProfile()">Save</button>
        <button class="btn btn-default" data-dismiss="modal">Close</button>
      </div>
    </div></div>
  </div>

  <!-- Logout Modal -->
  <div id="logoutModal" class="modal fade" tabindex="-1">
    <div class="modal-dialog"><div class="modal-content">
      <div class="modal-header"><h4>Confirm Logout</h4></div>
      <div class="modal-body"><p>Do you really want to logout?</p></div>
      <div class="modal-footer">
        <button class="btn btn-danger" onclick="confirmLogout()">Yes, Logout</button>
        <button class="btn btn-default" data-dismiss="modal">Cancel</button>
      </div>
    </div></div>
  </div>

  <!-- Scripts -->
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
  <script>
    function toggleSidebar() {
      $('#sidebar,#mainWrapper').toggleClass('collapsed');
    }
    function logout() {
      $('#logoutModal').modal('show');
    }
    function confirmLogout() {
      localStorage.removeItem('currentUser');
      window.location.href = '@Url.Action("Index","Account")';
    }
    function openProfileModal() {
      $('#profileModal').modal('show');
    }
    function saveProfile() {
      // your save‐profile logic
      $('#profileModal').modal('hide');
    }
  </script>
</body>
</html>
